<Page
    x:Class="ToDo_Desktop.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToDo_Desktop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/BackgroundTools.jpg" Stretch="UniformToFill" Opacity=".4"/>
        </Grid.Background>
        <StackPanel Background="WhiteSmoke" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Padding="30">
            <TextBlock Text="Login" Margin="0,0,0,50" HorizontalAlignment="Center"/>

            <StackPanel MinWidth="200">
                <TextBox Header="User name"/>
                <TextBox Header="Password"/>

                <Button x:Name="LoginBtn" Content="Log in" HorizontalAlignment="Center"
                    Click="LoginBtn_Clicked"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
